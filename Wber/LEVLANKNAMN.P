/*LEVLANKNAMN.P*/

DEFINE INPUT PARAMETER levnamnvar AS CHARACTER NO-UNDO.
DEFINE OUTPUT PARAMETER levkodvar AS CHARACTER NO-UNDO.
FIND FIRST LEVERANTOR  WHERE LEVERANTOR.LEVNAMN BEGINS levnamnvar NO-LOCK NO-ERROR.

IF NOT AVAILABLE LEVERANTOR THEN DO:
   RETURN.
END. 
levkodvar = LEVERANTOR.LEVKOD.