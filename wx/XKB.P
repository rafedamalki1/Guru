DEFINE VARIABLE kalkproch   AS HANDLE NO-UNDO. /* KALKAPP1.P */
RUN KALKAPP1.P PERSISTENT SET kalkproch.
FOR EACH OMRADETAB WHERE OMRADETAB.ELVOMRKOD = 1 NO-LOCK,
EACH FASTSPEC WHERE FASTSPEC.OMRADE = OMRADETAB.OMRADE NO-LOCK.
    
   RUN tabortkalkyl_UI IN kalkproch (INPUT FASTSPEC.KALKNR).
END.

 
