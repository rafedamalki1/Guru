/*DELALLKALK.P*/
OPEN QUERY DQ FOR EACH FASTSPEC NO-LOCK.
DO TRANSACTION:
   GET FIRST DQ EXCLUSIVE-LOCK.
   RUN DELFKALK.P
   (INPUT FASTSPEC.KALKNR).
   DELETE FASTSPEC.
END.
REPEAT :
   DO TRANSACTION:
      GET NEXT DQ EXCLUSIVE-LOCK.
      IF AVAILABLE FASTSPEC THEN DO:
         RUN DELFKALK.P
         (INPUT FASTSPEC.KALKNR).
         DELETE FASTSPEC.
      END.
      ELSE LEAVE.
   END.
END.
CLOSE QUERY DQ.
