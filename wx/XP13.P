DEFINE INPUT PARAMETER PKOD AS CHAR.
DEF VAR A1 AS DECIMAL.
DEF VAR A2 AS DECIMAL.
DEF VAR A3 AS DECIMAL.
DEF VAR A4 AS DECIMAL.
FOR EACH SUMTIDDAG WHERE YEAR(DATUM) = 2002 AND MONTH(DATUM) = 1 AND 
    PERSONALKOD = PKOD NO-LOCK:
    IF PRISTYP = "FRÅNVARO." THEN A1 = A1 + TIMMAR.
    ELSE IF PRISTYP = "TOT.PRIS." THEN A2 = A2 + TIMMAR.
    ELSE A3 = A3 + TIMMAR.
    DISPLAY AONR DELNR PRIS PRISTYP TIMMAR.
END.
DISPLAY A1 A2 A3.
