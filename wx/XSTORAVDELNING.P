/*
     Filename: XSTORAVDELNING.P
      Created: 03.08.0014 10:52ELPAO     
     Modified: 
*/
DEFINE VARIABLE avdnr AS INTEGER NO-UNDO.
DEFINE VARIABLE tempstring AS CHARACTER NO-UNDO.
OPEN QUERY aq FOR EACH STORDISTRIKT NO-LOCK BY AVDELNINGNR.
GET FIRST aq NO-LOCK.
tempstring = "".
avdnr = 0.
DO WHILE AVAILABLE(STORDISTRIKT):
   IF avdnr NE STORDISTRIKT.AVDELNINGNR THEN DO:
      avdnr = STORDISTRIKT.AVDELNINGNR.
      IF tempstring = "" THEN tempstring = STRING(STORDISTRIKT.AVDELNINGNR).
      ELSE tempstring = tempstring + ',' + STRING(STORDISTRIKT.AVDELNINGNR).
   END.
   GET NEXT aq NO-LOCK.
END.
CLOSE QUERY aq.
MESSAGE tempstring VIEW-AS ALERT-BOX.
