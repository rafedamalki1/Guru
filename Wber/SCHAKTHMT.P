/*SCHAKTHMT.P*/
{BERTEMPTAB.I}
DEFINE INPUT-OUTPUT PARAMETER TABLE FOR eurberedningtemp.
FOR EACH eurberedningtemp WHERE NO-LOCK:
   FIND FIRST HDSCHAKT WHERE HDSCHAKT.BERNR = eurberedningtemp.BERNR AND HDSCHAKT.OMRADE = eurberedningtemp.OMRADE 
   NO-LOCK NO-ERROR.
   IF NOT AVAILABLE HDSCHAKT THEN DELETE eurberedningtemp.   
END.